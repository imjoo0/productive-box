name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.socket io 비동기 처리 }}
          TIMEZONE: Asia/Seoul
